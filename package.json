{
  "name": "chatwidget",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "bun --filter client --filter server dev",
    "dev:client": "bun --filter client dev",
    "dev:server": "bun --filter server dev",
    "build": "bun --filter client build",
    "typecheck": "bun --filter server typecheck && bun --filter client run build",
    "clean": "rm -rf node_modules client/node_modules server/node_modules shared/node_modules client/dist bun.lock"
  }
}
